// Question ==>  Use library functions to calculate the square of number given by user.


#include<stdio.h>
#include<math.h>

void calculatePrice (float value);

int main(){
    int n=4;
    printf("%f", pow(n,2));
    return 0;
}